# Node / Astro — ignore build output and deps
node_modules/
dist/
.astro/
dev-dist/

# Re-include the CMS API client source dir, which the repo-root Python-template
# `lib/` rule would otherwise swallow (it's meant for Python build artifacts).
!src/lib/
!src/lib/**